[package]
name = "focal-vector-protocol"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Versioned local IPC types for Focal Vector"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
